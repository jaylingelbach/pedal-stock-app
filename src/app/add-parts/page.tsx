import { AddPartsForm } from './components/PartsInventoryForm';

/**
 * Page component that renders the AddPartsForm UI.
 *
 * @returns A React element containing the `AddPartsForm` component.
 */
export default function PartsInventoryPage() {
  return <AddPartsForm />;
}
