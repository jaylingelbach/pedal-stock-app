import { PartsBin } from './components/PartsBin';

/**
 * Page component that renders the PartsBin UI.
 *
 * @returns A React element containing the `PartsBin` component
 */
export default function PartsBinPage() {
  return <PartsBin />;
}
